namespace ElevatorSystem.Models;

public class Enums
{
    public enum Direction
    {
        Up,
        Down,
        Idle
    }

    public enum ElevatorState
    {
        Idle,
        Moving,
        Loading
    }
}
